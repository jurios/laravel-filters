<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Filter prefix
    |--------------------------------------------------------------------------
    |
    | You can define a global prefix that your filters will have in the query parameters URL in order to
    | detect which query parameters are filters and which aren't. This prefix can be also defined once you instances
    | the filter class and only take effect during the class instance.
    |
    */
    'prefix' => null
];